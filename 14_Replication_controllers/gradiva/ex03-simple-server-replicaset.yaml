apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: simple-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: simple-server
  template:
    metadata:
      labels:
        app: simple-server
    spec:
      containers:
      - name: simple-server
        image: leon11sj/test-app
